name: Deploy
on:
  workflow_run:
    workflows:
      - Test
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        uses: actions/checkout@v4
      - name: Run deploy.py
        run: |
          ./ci/scripts/deploy.py